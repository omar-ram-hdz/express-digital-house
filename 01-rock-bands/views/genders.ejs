<!DOCTYPE html>
<html>
  <head>
    <%if(!error && gender){%>
    <title><%=gender%> | Bands</title>
    <%}if(error){%>
    <title>Bands</title>
    <%}%>
    <link rel="shortcut icon" href="/images/guitar.ico" type="image/x-icon" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <%- include("./partials/header.ejs") %>
    <main>
      <%if(!error && data && gender){%>
      <h2>Genero: <%=gender%></h2>
      <%- include("./partials/bands.ejs") %> <%}%> <%if(error){%>
      <h2>Ocurrió un error</h2>
      <p><%=messageError%></p>
      <%}%>
    </main>
    <!-- 
      - Home -> Lista de bandas
      - Band :id -> Banda
      - Géneros -> Lista de géneros
      - Genero :id -> Bandas de un genero específico
    -->
    <%- include("./partials/footer.ejs") %>
  </body>
</html>
